# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/

# uv
uv.lock

# pytest
.pytest_cache/
# --basetemp lands here; the suffixed forms are what you reach for when a
# previous run still holds a lock on the plain one, so ignore those too.
.pytest-tmp*/

# cflow run state (per-directory, machine-local)
.cflow/

# OS
.DS_Store
Thumbs.db
